{
  "publicToken": "",
  "note": "Deployment-only public pk token with styles:read, fonts:read and styles:tiles, restricted to the production origin. Never use an sk token or commit the runtime config. Native Mapbox GL is primary; unsupported WebGL/provider failures use an explicitly labelled Leaflet fallback."
}
