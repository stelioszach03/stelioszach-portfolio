{
  "name": "stelios-zacharioudakis-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/assets/site.js && node --check scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
